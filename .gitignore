.DS_Store
node_modules/
.vercel

# Supabase CLI local state (machine-specific; can hold project credentials)
supabase/.temp/
supabase/.branches/
.env
.env.local

# Raw form exports: contain student names, emails, phone numbers. Never commit.
*.csv
uploads/*.csv

# Same student contact details, one step downstream: the generated import.
supabase/insert-student-led-groups.sql

# Raw form exports, xlsx flavour: same student names, emails, phone numbers.
*.xlsx
uploads/*.xlsx
